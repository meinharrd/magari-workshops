machine:
  timezone:
    UTC
  php:
    version: 5.4.15
database:
  post:
    - app/console app/console doctrine:database:create --env=test
    - app/console app/console doctrine:schema:create --env=test
test:
  override:
    - phpunit -c app
